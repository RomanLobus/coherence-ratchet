```json
{"verdict": "CONSOLIDATE", "reason": "These functions are near-identical wrappers around a common `request()` function that differ only in the HTTP method name and parameter signatures, making them ideal candidates for consolidation via a factory function or decorator pattern.", "confidence": "high"}
```