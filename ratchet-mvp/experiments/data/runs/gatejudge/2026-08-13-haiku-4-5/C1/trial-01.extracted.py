```json
{"verdict": "CONSOLIDATE", "reason": "These functions are near-identical wrappers around a common `request()` call that differ only in HTTP method and parameter signatures, making them ideal candidates for consolidation into a factory function or decorator pattern.", "confidence": "high"}
```