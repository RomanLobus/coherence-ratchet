```json
{
  "pattern": "http-verb-shortcut",
  "reason": "These are module-level HTTP verb helpers (get, post, put, patch) that each set a method string and delegate to the request() function with near-identical bodies, matching the documented sanctioned pattern for symmetric public API design.",
  "confidence": "high"
}
```